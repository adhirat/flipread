{
  "name": "flipread",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute flipread-db --local --file=./src/db/schema.sql",
    "db:migrate:prod": "wrangler d1 execute flipread-db --remote --file=./src/db/schema.sql",
    "test": "node --experimental-strip-types --test"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.65.0"
  }
}
